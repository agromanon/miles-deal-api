const express = require('express');
const router = express.Router();

// Placeholder para admin (implementar depois)
router.get('/stats', (req, res) => {
  res.json({ message: 'Admin stats - em desenvolvimento' });
});

module.exports = router;
